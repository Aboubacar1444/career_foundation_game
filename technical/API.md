# API

## Principes
API versionnée, authentification sécurisée, validations serveur, idempotence sur les achats et actions importantes.

## Exemples V0
GET /career
GET /world
GET /profile
GET /songs
POST /songs
POST /releases
POST /concerts
GET /charts
GET /quests
POST /quests/{id}/actions
GET /events
POST /events/{id}/choice
GET /notifications
